{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-fluid">
        <h1>Recherche</h1>
        <h2>Résultats</h2>
        <div class="row">
            <div class="col-12">
                <div class="row">
                    {% for result in results['data'] %}
                        <div class="col-6 col-md-4 col-xl-3">
                            <div class="card media-card mb-4">
                                <form action="{{ path('album_new') }}" method="post">
                                    <input type="hidden" id="title" name="title" value="{{ result.title }}">
                                    <input type="hidden" id="poster" name="poster" value="{{ result.cover_big }}">
                                    <input type="submit" value="Ajouter" class="ml-2 btn btn-outline-success">
                                </form>
                                <img class="card-img-top"
                                     src="{{ result.cover_big }}">
                                <div class="card-body">
                                    <h5 class="card-title d-flex justify-content-center">{{ result.title }}</h5>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}