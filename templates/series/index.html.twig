{% extends 'base.html.twig' %}

{% block title %}Tous les films{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Toutes les séries</h1>
        <div class="row">
            {% for serie in series %}
                <div class="col-6 col-md-4 col-xl-3">
                    <div class="card media-card mb-4">
                        <form method="post" action="{{ path('series_delete', {'id': serie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serie.id) }}">
                            <button class="btn btn-outline-danger">Delete</button>
                        </form>
                        <img class="card-img-top" src="{{ serie.poster }}">
                        <div class="card-body">
                            <h5 class="card-title d-flex justify-content-center">{{ serie.title }}</h5>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
